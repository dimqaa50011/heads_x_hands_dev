package creatures;

public class Monster extends Creatures{
    public Monster(String name, int attack, int armor, int health, int maxDamage) {
        super(name, attack, armor, health, maxDamage);
    }
}
