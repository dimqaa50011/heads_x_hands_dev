package creatures;

public class Player extends Creatures {
    public Player(String name, int attack, int armor, int health, int maxDamage) {
        super(name, attack, armor, health, maxDamage);
    }
}
